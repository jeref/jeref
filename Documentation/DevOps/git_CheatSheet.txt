***
revert last commit :
git reset HEAD~

***
merge feature branch (with GH CLI as git request-pull don't works to me) :
gh pr create --base develop --head feature_deployment


https://stackoverflow.com/questions/25356810/git-how-to-squash-all-commits-on-branch
defaultBranch=$(git config --get init.defaultBranch || echo develop)
echo $defaultBranch

git checkout yourBranch
git merge-base develop $(git branch --show-current) ==> copy SHA
git log | less ==> search SHA and check is first commit to squash
git reset $(git merge-base develop $(git branch --show-current))
git add -A
git commit -m "one commit on yourBranch"

*********************************
delete local branch no longer on remote :
https://stackoverflow.com/questions/7726949/remove-tracking-branches-no-longer-on-remote
==> 2 steps before to check : 
1- git remote prune origin (== git fetch -p ?)
2- git fetch -p && for branch in $(git for-each-ref --format '%(refname) %(upstream:track)' refs/heads | awk '$2 == "[gone]" {sub("refs/heads/", "", $1); print $1}'); do echo "will delete $branch"; done
or git branch and git branch -r and visual compare
==> git fetch -p && for branch in $(git for-each-ref --format '%(refname) %(upstream:track)' refs/heads | awk '$2 == "[gone]" {sub("refs/heads/", "", $1); print $1}'); do git branch -D $branch; done

